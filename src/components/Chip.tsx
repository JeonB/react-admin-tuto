import React from "react";
import { Translate } from "@mui/icons-material";
import {
  Button,
  Chip,
  Collapse,
  Dialog,
  DialogActions,
  DialogContent,
  DialogTitle,
  FormControl,
  InputLabel,
  MenuItem,
  Popover,
  Select,
  Stack,
  TextField,
  Typography,
  useTheme,
} from "@mui/material";

<Chip
  label={getLabel()}
  // color="primary"
  // variant="outlined"
  size="small"
  onClick={handleClick}
  onDelete={(event) => handleHide(event, source)}
/>;
